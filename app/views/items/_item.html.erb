<html lang="en-GB">
  <div id="<%= dom_id item %>">
    <p>
      <strong>Name:</strong>
      <%= item.name %>
    </p>

    <p>
      <strong>Description:</strong>
      <%= item.description %>
    </p>

    <p>
      <strong>URL:</strong>
      <%= item.URL %>
    </p>

    <p>
      <strong>Tags:</strong>
      <%= item.tags %>
    </p>
  </div>
</html>